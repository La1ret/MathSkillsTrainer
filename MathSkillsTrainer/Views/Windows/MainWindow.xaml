<Window x:Class="MathSkillsTrainer.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MathSkillsTrainer.ViewModels"
        xmlns:local="clr-namespace:MathSkillsTrainer"
        mc:Ignorable="d"
        Title="Основа" Height="450" Width="800">
    <Grid>
        <Button Content="←" Style="{StaticResource TransparentButton}" Command="{Binding BackToAuthCommand}" Background="PaleTurquoise"/>
        <TextBlock Text="{Binding UserList}"/>
    </Grid>
</Window>
